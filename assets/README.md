# Assets

Shopscape draws **all** of its art procedurally on an HTML5 canvas — characters, monsters, terrain, and UI are vector shapes generated at runtime, so there are currently **no binary image assets** to manage.

This folder is reserved for future assets (sprites, audio, icons, fonts) as the project grows. See `docs/ROADMAP.md`.
